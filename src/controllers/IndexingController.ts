import {IController} from "../models"

export const IndexingController: IController = {
    get(req, res) {
        // TODO create scraping logic
        return res.send("TODO: create scraping logic")
    }
}